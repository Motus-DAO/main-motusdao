# Visual tokens

**Canonical:** MotusDAO Operational Design System (Main Site)  
Path: `MotusDAO/deliverables/Design system/docs/04-design-system-operativo.md`

Implemented in `app/globals.css` + `tailwind.config.ts`.

## Main Site rules

- Purple–pink gradient primary (`--grad-brand`)
- Dark nav + footer always (`.chrome-dark`)
- No cyan, no Orbitron, no terminal
- Optional light content mode via `html.light`
- Ambient: `--grad-bg-ambient`
- Type: Jura (`--font-heading`) + Inter (`--font-body`)
